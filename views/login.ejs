<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="stylesheet" href="/normalize.css" />
    <link rel="stylesheet" href="/main/styles.css" />
    <link rel="stylesheet" href="/forms/forms.css" />
    <title><%= pageTitle %></title>
  </head>
  <body>
    <!--ref the nav bar-->
    <header><%- include("../views/includes/nav.ejs") %></header>
    <main>
      <div id="content">
        <div class="form-container">
          <!--form to get user logged in doesn't use autofill
          uses Access ID to login - already a unique identifier-->
          <form action="/login" method="post" autocomplete="off">
            <div class="form-control">
              <label for="orgId">Access ID</label>
              <input name="orgId" type="text" />
            </div>
            <div class="form-control">
              <label for="password">Password</label>
              <input name="password" type="password" />
            </div>
            <input type="hidden" name="_csrf" value="<%=csrfToken %>" />
            <div classame="formSubmit">
              <button class="btn" type="submit">Login</button>
            </div>
          </form>
        </div>
      </div>
    </main>
  </body>
</html>
